import 'package:flutter/material.dart';

class DeliveryCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Delivery Calendar Screen'),
    );
  }
}
